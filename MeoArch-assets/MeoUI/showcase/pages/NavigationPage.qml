import QtQuick
import QtQuick.Controls
import MeoUI

Item {
    anchors.fill: parent
    Text {
        text: "Navigation Components Demo (See App Layout)"
        anchors.centerIn: parent
        font.pixelSize: 20 * MeoTheme.globalScale
        color: MeoTheme.onSurface
    }
}
