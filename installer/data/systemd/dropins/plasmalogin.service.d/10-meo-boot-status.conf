[Unit]
# Plasma Login Manager starts after plymouth-quit.service, so a pre-start
# Plymouth progress update would be invisible. Keep only the failure hook for
# diagnostic state recording.
OnFailure=meo-boot-status-failure@plasmalogin.service
