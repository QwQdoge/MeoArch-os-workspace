# Themes & UI Components Management

`themes/MeoUI` 和 `themes/MeoUI-SpecSite` 包含 MeoArch OS 系统安装器及展示站使用的 UI 样式组件。

> [!NOTE]
> 核心组件库的权威开发源在顶级目录下的 **`meo-ui/`**（即原本的 `MeoUI` 项目）中。
> 在修改底层 QML 控件或组件库逻辑时，请优先在 `meo-ui/` 仓库中维护更新，然后同步构建/复用到本主题目录中。
